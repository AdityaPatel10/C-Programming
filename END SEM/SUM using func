#include <stdio.h>
int add(int* , int*);
int main ()  {
    int a = 10 , b = 10;
    int sum = add(&a , &b);
    printf ("sum = %d", sum);
    return 0;
}
int add(int*a , int*b)
{
    int sum = *a + *b;
    return sum;   
}
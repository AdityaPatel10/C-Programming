#include <stdio.h>
int main ()   {
    
     //for (initialization ; condition ; updation)   

    for (int i = 1 ; i <=100 ; i++)         
     // increment operator

    //  pre inc.. = ++i   (first increase, then use)

   // post inc.. = i++  (first use, then increase)

    printf ("%d \n",i);
    return 0;
}